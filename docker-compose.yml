version: "3.8"

services:
  cfnb:
    build: .
    container_name: cfnb-dashboard
    ports:
      - "5000:5000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - TZ=Asia/Shanghai
